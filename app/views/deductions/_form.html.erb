<%= bootstrap_form_for(deduction) do |f| %>
  <% if deduction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deduction.errors.count, "error") %> prohibited this deduction from being saved:</h2>

      <ul>
      <% deduction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.collection_select :equipment_id, Equipment.all, :id, :name %>
  </div>

  <div class="field">
    <%= f.text_field :link_to_return %>
  </div>

  <div class="field">
    <%= f.text_field :elected_cost %>
  </div>

  <div class="field">
    <%= f.text_field :classification %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
