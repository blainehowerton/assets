<%= form_for(deduction) do |f| %>
  <% if deduction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deduction.errors.count, "error") %> prohibited this deduction from being saved:</h2>

      <ul>
      <% deduction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :equipment_id %>
    <%= f.number_field :equipment_id %>
  </div>

  <div class="field">
    <%= f.label :link_to_return %>
    <%= f.text_field :link_to_return %>
  </div>

  <div class="field">
    <%= f.label :elected_cost %>
    <%= f.text_field :elected_cost %>
  </div>

  <div class="field">
    <%= f.label :classification %>
    <%= f.text_field :classification %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
