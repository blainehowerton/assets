<%= bootstrap_form_for(equipment) do |f| %>
  <% if equipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment.errors.count, "error") %> prohibited this equipment from being saved:</h2>

      <ul>
      <% equipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
    <%= f.text_field :name %>
    </div>

    <div class="field">
    <%= f.text_area :description %>
    </div>

    <div class="field">
    <%= f.date_field :in_service_date %>
    </div>

    <div class="field">
    <%= f.text_field :serial %>
    </div>

    <div class="field">
    <%= f.text_field :amount %>
    </div>

    <div class="field">
    <%= f.text_field :link_url %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
